# Database Configuration (Required)
# Get your connection string from Neon Console (https://neon.tech)
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# JWT Secret for Authentication (Required)
# Generate a secure random string for production
JWT_SECRET=your-secret-key-change-in-production

# External API URL (Optional - only needed during migration)
# This is the external backend that we're replacing
NEXT_PUBLIC_API_URL=https://tm-backend-qfaf.onrender.com

# File Upload Storage (Optional - for future image uploads)
# Uncomment and configure if using cloud storage
# STORAGE_BUCKET_URL=
# STORAGE_ACCESS_KEY=
# STORAGE_SECRET_KEY=
